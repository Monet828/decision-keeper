# Decisions

2026-09-21: AI_dev_template fullを利用し、標準app/docs/memory/scripts構造を維持。大会対応はdocs/hackathonとdocs/submission、比較デモ用fixtures、検証artifactsの追加で行う。これはセットアップ作業上の判断。
空き箱・IgNightの既存リポジトリをコピーせず参照先として記録。両方の現行実装状態は未確認。
コンセプトへの賛同と製品要件・技術設計の承認を区別。mvp-v0.1は候補として保存。

## 2026-09-21 セッション2: 方向性の確認（ユーザー選択）
出典: Claude Codeセッションでの選択式確認。ユーザーが4項目を明示選択した。

- North Star: 「CIは通るが、チームが守っていた技術判断（の前提）を壊す変更」を、
  過去判断の前提が現在も成立するかという観点で検出し、継承／更新提案／保留に振り分ける。
- D-01 [確定] MVPの成果物は「判断レポートまで」。修正コード生成・PR作成はMVPに含めない。
- D-02 [確定] 実装形態は Python CLI + JSON/YAML の判断資産。Web UIは作らない。出力はMarkdown。
- D-03 [確定] モデル呼び出しは OrcaRouter 経由。APIキーはユーザーがローカル環境変数に設定する。
  キーの存在・疎通は未検証。呼び出し実装の前に疎通確認を行う。
- D-04 [確定] folder-lens / ignight は先に読み取り専用で調査し、再利用範囲はその結果で決める。
  両リポジトリは変更しない。

制約として共有済み: 提出期限 2026-09-22 15:00（資料記載、タイムゾーン未明記）。
残り時間が約1日である前提で範囲を絞る。
