# 承認記録 — MVP要件

古い記録は書き換えず追記する。

---

## 2026-09-21 / `docs/specs/mvp-v0.2.md` 版 0.2.0 / §5 要件 R-01〜R-10

**対象**: `docs/specs/mvp-v0.2.md` の §5 要件のみ。
**承認されていないもの**: §4のCLI引数と終了コード、判断資産YAMLのスキーマ、
追加する外部依存、OrcaRouterへの実呼び出しと予算（すべて §7 の「確認する」側）。

**承認時のコミット**: `8fbdb7f` (chore: scaffold AI HACK 2026 project and record MVP spec v0.2)

**ユーザー発言**（2026-09-21、Claude Codeセッション）:
> 自分のリポジトリだから問題ない、そのまま進めて。今回の大会用のレポジトリは新しくpublicで作って欲しい。あと、oraca routerのAPIは発行済みだから、後で共有するね

**この発言で確定したこと**:
- folder-lens のスキーマ語彙を本リポジトリで再利用してよい（所有権はユーザー本人、第三者権利の問題なしとの申告）
- §5 要件 R-01〜R-10 を要件として承認
- 大会用に新規 public リポジトリを作成する
- OrcaRouter APIキーは発行済み。共有は後日

**注記**: 承認時点でコミット `8fbdb7f` には R-01〜R-10 が `[候補]` 表記のまま含まれる。
承認を受けて `[確定]` に更新したのは後続コミット。差分は変更履歴に残す。

---

## 2026-09-21 / `docs/specs/mvp-v0.2-design.md` / 設計承認

**対象**: `docs/specs/mvp-v0.2-design.md` 全体。判断資産YAMLスキーマ、処理フロー、
決定論的な総合判定規則、安全機構、追加依存3点（pydantic / pyyaml / httpx）、
ディレクトリ構成 `app/decision_keeper/`。

**承認時のコミット**: `2f5d30b` (docs: record requirement approval and add design proposal)

**ユーザー発言**（2026-09-21、Claude Codeセッション）:
> 1はそれで進めていいよ。記事については同時並行で進めているから、後で合流させる。

（「1」= 直前のメッセージで提示した「1. 設計案の承認」を指す）

**この発言で確定したこと**:
- 設計案の実装着手を承認
- 記事（Qiita/Zenn）はユーザーが並行執筆。エージェント側では書かず、後で合流させる
- 提出フォームURLの共有（`docs/hackathon/event-requirements.md` に記録、非公開）

**未承認のまま残るもの**: OrcaRouterへの実呼び出しと予算（§7）。キー共有後に別途確認する。
