# OrcaRouterの接続準備

クレジット解放済み（2026-09-21のユーザー申告）。登録やクレジット解放を再依頼しない。キーや残高は未確認。
`.env.example` は今回定めた環境変数名の候補であり、公式SDKの自動認識を意味しない。アプリ側で明示的に読み込む。
実装時に適切なローカル.envへ設定し、キーの値をチャット・記事・ログに出さない。クライアント公開変数にしない。
Base URLは大会PDF p.18の `https://api.orcarouter.ai/v1`。モデル候補は `orcarouter/auto`。最適モデルとして確定していない。

実装時には公式 https://docs.orcarouter.ai を確認し、選定SDK、レスポンスのモデル名、利用量、エラー、Fallback条件を検証する。
予算・呼び出し上限を決めたうえで最小の疎通確認を行う。今回キー作成・ライブAPI呼び出し・課金テストはしていない。
